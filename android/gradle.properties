org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Disable Kotlin incremental compilation. On Windows the incremental cache crashes
# ("Could not close incremental caches" / "different roots") when the pub cache and
# project live on different drives. Turning it off bypasses the broken cache; builds
# are slightly slower but reliable. (Safe everywhere.)
kotlin.incremental=false
